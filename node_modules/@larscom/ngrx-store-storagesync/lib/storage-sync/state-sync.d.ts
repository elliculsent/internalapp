import { IStorageSyncOptions } from './models/storage-sync-options';
/**
 * @internal Blacklisting
 * @returns the filtered state
 */
export declare const excludeKeysFromState: <T>(state: Partial<T>, excludeKeys?: string[]) => Partial<T>;
/**
 * @internal Remove empty objects from state
 * @returns the cleaned state
 */
export declare const cleanState: <T>(state: Partial<T>) => Partial<T>;
/**
 * @internal Sync state with storage
 * @param state the next state
 * @param options the configurable options
 * @returns the next state
 */
export declare const stateSync: <T>(state: T, { features, storage, storageKeySerializer, storageError, version: currentVersion }: IStorageSyncOptions<T>) => T;
